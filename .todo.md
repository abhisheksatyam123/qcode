## Systems

### Goal
Implement SQLite persistent session storage, session management slash commands (/new, /sessions, /session), colored git diffs, and exact opencode tool call observability views.

### Architecture
- **SQLite Database** (`include/ai/tui/db.h`, `src/tui/db.cpp`):
  - Database manager targeting `~/.qcode.db`.
  - Tables: `sessions` (id, title, provider, model, created_at) and `messages` (id, session_id, sender, content, created_at).
  - Persistence lifecycle: loads last session on startup, automatically saves user prompt, tool calls, tool results, and assistant replies.
- **State management** (`include/ai/tui/state.h`):
  - `ChatState` updated with `session_id` (shared_ptr<string>) to track active session persistence.
- **Commands backend** (`src/tui/commands.cpp`):
  - `/new`: Creates a new session UUID, saves it to SQLite, and clears chat logs.
  - `/session <id>`: Switches active session and reloads history.
  - `/theme <name>`: Dynamic color theme switching.
  - `/tools [on|off]`: Toggle tools and real-time streaming mode in TUI.
- **Observability views** (`src/tui/tools.cpp`):
  - Outputs formatted tool blocks: `▾ Observability · <tool_name>` and `$ <command>` (bash) or `↳ Spawn Task` (task), with dynamic return output lines.
- **UI Views** (`src/tui/views.cpp`):
  - Left-bordered user/assistant logs, colored git diff files tab scroll feed.
  - Popups optimized with FTXUI `dbox` overlays to prevent layout shifts.
  - Git status diff queries cached dynamically using modification time checks to ensure high responsiveness.
  - Autocomplete suggestion list rendered dynamically inline directly stacked above the text input bar.
  - Auto-query session persistence to suggest and auto-complete session UUIDs dynamically when typing `/session `.
  - Floating session selector popup window matching the exact style and navigation of the model selector popup.

### Status
- Base Porting: ✅ Complete
- Theme Color Switcher: ✅ Complete
- Left-border Chat Blocks: ✅ Complete
- Scrolling & Auto-scroll: ✅ Complete
- Persistent SQLite Session Storage: ✅ Complete
- Session Slash Commands (/new, /tools): ✅ Complete
- Exact Tool Call Observability View: ✅ Complete
- Fluid Overlays (No Layout Shift Popups): ✅ Complete
- High Responsiveness Diff Caching: ✅ Complete
- Dynamic Inline Autocomplete & Vim Navigation: ✅ Complete
- Persistent SQLite Session Selection Autocomplete: ✅ Complete
- Floating SQLite Session Selector Dialog Window: ✅ Complete
- Redundant /sessions Command Clean up: ✅ Complete
- First-Match Auto-Autocomplete on Return/Tab: ✅ Complete
- Direct Return Autoselect-and-Execute Popups: ✅ Complete

## Done

- [x] Fetch SQLite static library target in CMake
- [x] Add `db.h` and `db.cpp` to manage SQLite connection, schema creation, message saving, and history reloading
- [x] Initialize `db` in `main.cpp`, load active session, and link `sqlite3` to TUI target
- [x] Save user messages and assistant responses dynamically into `messages` table in SQLite
- [x] Implement slash commands `/new`, `/tools`, and `/session <id>` in `commands.cpp`
- [x] Refactor `format_tool_call` and `format_tool_result` in `tools.cpp` to look like opencode observability format
- [x] Implement `dbox` overlays in `views.cpp` for model select and slash command popups to prevent layout shifting
- [x] Optimize file diff lookups with timestamp caching to remove CPU lag during text editing and screen redraws
- [x] Implement dynamic inline slash command autocomplete stacked above the input box matching opencode layout
- [x] Support Vim keybindings (j/k) and arrow keys to navigate inline slash command autocomplete suggestions
- [x] Add dynamic inline SQLite session loading suggestions when typing `/session ` to switch sessions seamlessly
- [x] Implement a dedicated floating session selector popup window for `/session` (similar to model selector) using `dbox` overlay and supporting Vim keys
- [x] Remove the redundant `/sessions` command and consolidate session listing/loading into the `/session` selector popup
- [x] Enable automatic first-match autocomplete fill on Return or Tab keypresses without requiring manual arrow navigation
- [x] Trigger direct autoselect and execution of overlay popups (e.g. `/session` or `/model`) immediately on autocomplete Return, removing the need for double Enter presses
